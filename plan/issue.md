[x] T1. 输入结构发生变化，现在更新为一个 xlsx 文件加上一个 `附件/` 文件夹。是否需要更新。

已完成（2026-09-13）：
- `ScanInput` 同时扫描输入根目录与 `附件/`，附件支持松散 .xls/.xlsx 与压缩包；同目录下已有解压附件时跳过压缩包
- 映射表按表头文字定位列（姓名/学号/教学安排表），兼容旧版 5 列与新版 6 列；不再写死第几列
- 附件名匹配：归一化（全角空格→半角、合并空白）精确匹配 + 去掉提交者前缀后唯一匹配兜底
- zip 内 GBK 文件名解码；不再跳过 `.` 开头的合法附件（仅跳过 `__MACOSX`）
- 二进制 Excel（3 个真 .xlsx + 2 个 Excel 97-2003 .xls）跳过并在末尾列出需重收名单
- 旧结构回归验证：`input_20251` 41/41 全部导入成功
- 本次 20260 结果：29 人成功导入，5 人需重新收集（其中 2 人是用截图贴进 Excel 的课表）
- 附带修复：二维表因内含「上课班级代码」小表格被误判为列表格式（影响 1 人）